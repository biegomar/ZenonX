using UnityEngine;

namespace Enemies.Services.Formations
{
    public class BaseEnemyFormation : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
